import styled from 'styled-components';
import { cursorStyle } from '../../GlobalStyle';

export const LogoStyle = styled.img`
  max-width: 60px;
  width: 100%;
  margin-right: 20px;
  ${cursorStyle}
`;
